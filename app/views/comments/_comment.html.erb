<% @comments.each do |comment| %>  
      <div class="media">
        <div class="media-body">
          <small>
            <%= comment.body %>
       submitted <%= time_ago_in_words(comment.created_at) %> ago by
            <%= %>
     </small>
          </small>
        </div>
      </div>
    <% end %>
